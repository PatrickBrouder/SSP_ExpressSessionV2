extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="loginForm", method="post")
        input(type="text", name="username")
        input(type="password", name="password")
        input(type="submit")
